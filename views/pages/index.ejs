<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= siteName %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">
  <%- include('../partials/navbar', { user: user }) %>
  <div class="container">
    <div class="text-center py-5">
      <h1 class="display-4 fw-bold">Nargift Giveaway</h1>
      <p class="lead">Çekilişlerinizi kolayca oluşturun, yönetin ve kazananları duyurun!</p>
      <a href="/giveaway/create" class="btn btn-modern btn-lg mt-3">Çekiliş Oluştur</a>
    </div>
    <% if (giveaways && giveaways.length > 0) { %>
      <h3 class="mb-3">Aktif Çekilişler</h3>
      <div class="row">
        <% giveaways.filter(g => g.status === 'active').forEach(giveaway => { %>
          <div class="col-md-4 mb-4">
            <div class="card card-modern h-100 border-0">
              <% if (giveaway.banner) { %>
                <img src="<%= giveaway.banner %>" class="card-img-top" style="height:180px;object-fit:cover;">
              <% } %>
              <div class="card-body">
                <h5 class="card-title"><%= giveaway.title %></h5>
                <p class="card-text"><%= giveaway.description %></p>
                <p class="mb-1"><span class="badge badge-modern">Kazanan: <%= giveaway.winnerCount %></span></p>
                <a href="/giveaway/<%= giveaway.id %>" class="btn btn-modern btn-sm mt-2">Detay</a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="alert alert-secondary text-center">Henüz aktif çekiliş yok.</div>
    <% } %>
  </div>
  <footer class="text-center text-secondary py-4 mt-5">
    &copy; <%= new Date().getFullYear() %> Nar Gift
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 